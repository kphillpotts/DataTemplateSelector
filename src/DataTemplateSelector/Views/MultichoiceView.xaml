<?xml version="1.0" encoding="UTF-8" ?>
<ViewCell x:Class="DataTemplateSelector.Views.MultichoiceView" xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml">

    <Grid Padding="10" BackgroundColor="#82b1ff">
        <Grid.RowDefinitions>
            <RowDefinition Height="AUTO" />
            <RowDefinition Height="AUTO" />
            <RowDefinition Height="AUTO" />
            <RowDefinition Height="AUTO" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="AUTO" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Label Grid.ColumnSpan="2" Text="{Binding Question}" />

        <Switch Grid.Row="1" Grid.Column="0" IsToggled="{Binding Option1Value}" />
        <Label Grid.Row="1" Grid.Column="1" Text="{Binding Option1Text}" VerticalOptions="Center" />
        <Switch Grid.Row="2" Grid.Column="0" IsToggled="{Binding Option2Value}" />
        <Label Grid.Row="2" Grid.Column="1" Text="{Binding Option2Text}" VerticalOptions="Center" />
        <Switch Grid.Row="3" Grid.Column="0" IsToggled="{Binding Option3Value}" />
        <Label Grid.Row="3" Grid.Column="1" Text="{Binding Option3Text}" VerticalOptions="Center" />
    </Grid>

</ViewCell>
